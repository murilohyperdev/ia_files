spring:
  jpa:
    show-sql: true

app:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:3001

logging:
  level:
    com.minhaempresa.meusaas: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
